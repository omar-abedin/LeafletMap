Data Desciption
Data sets related to the 50 states of the United States of America.

Population:

population estimate as of July 1, 1975
Income:

per capita income (1974)
Description
Color Depth

Income per capita of State
Size of Circle

Population of State
library(leaflet)
## Warning: package 'leaflet' was built under R version 3.3.2
library(htmltools)
## Warning: package 'htmltools' was built under R version 3.3.2
data(state)

state.info <- data.frame(state.x77)
state.info$State.Name = rownames(state.info)
state.info$lng = state.center$x
state.info$lat = state.center$y

pal <- colorNumeric(
  palette = "Blues",
  domain = state.info$Income
)

#df <- data.frame( lng = state.center$x , lat = state.center$y, name = state.name, pop = state.x77)
m <- state.info %>% leaflet() %>%  addTiles() %>%   addCircleMarkers(lng = ~lng, lat = ~lat, popup = ~State.Name, clusterOptions = markerClusterOptions(), radius = ~((Population/sum(state.info$Population))*1000), color =~pal(Income))
m
